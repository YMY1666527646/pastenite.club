bool InitializeHack()
{
	{
		Utilities::OpenConsole();
		Utilities::SetConsoleColor(BACKGROUND_GREEN | BACKGROUND_INTENSITY);
		Utilities::SetConsoleColor(FOREGROUND_INTENSE_WHITE);
		Utilities::Log("injected pastenite.club");
		Utilities::Log2("	");
		int backend = 1;
	}
  
  // you need backend for it, which i will upload at 5 stars hehe
